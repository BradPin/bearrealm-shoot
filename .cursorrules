# Cursor & Agent Entry Point

> **SSOT**: `.agent/rules.md` — All rules, standards, and skill dispatch.

**Prefer `.cursor/rules/*.mdc`** for context-aware conditional loading.
This file exists as a legacy fallback. The `.cursor/rules/global.mdc` provides the same dispatch with lower token cost via glob-based loading.
